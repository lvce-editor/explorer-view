import type { ExplorerState } from '../ExplorerState/ExplorerState.ts'

export const handleDoubleClick = async (state: ExplorerState): Promise<ExplorerState> => {
  // TODO if index is -1, enter editing mode

  return state
}
