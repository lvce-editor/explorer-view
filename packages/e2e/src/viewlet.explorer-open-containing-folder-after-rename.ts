import type { Test } from '@lvce-editor/test-with-playwright'

export const name = 'viewlet.explorer-open-containing-folder-after-rename'
export const skip = 1

export const test: Test = async () => {
  // TODO arrange: create and rename a nested file in Explorer.
  // TODO act: invoke open containing folder or reveal for the renamed file.
  // TODO assert: Explorer targets the renamed path and expands the correct parent folder.
}
