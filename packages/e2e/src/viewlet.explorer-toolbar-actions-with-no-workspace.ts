import type { Test } from '@lvce-editor/test-with-playwright'

export const name = 'viewlet.explorer-toolbar-actions-with-no-workspace'
export const skip = 1

export const test: Test = async () => {
  // TODO arrange: open Explorer with no workspace folder.
  // TODO act: invoke toolbar actions that should be disabled or inert.
  // TODO assert: the welcome or empty state remains stable and no errors are shown.
}
