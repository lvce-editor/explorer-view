import type { Test } from '@lvce-editor/test-with-playwright'

export const name = 'viewlet.explorer-rename-open-file-updates-tab-title'
export const skip = 1

export const test: Test = async () => {
  // TODO arrange: create a file, open it from Explorer, and verify the tab is visible.
  // TODO act: rename the open file from Explorer.
  // TODO assert: the Explorer row, editor tab, and editor title all use the new file name.
}
