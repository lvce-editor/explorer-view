import type { Test } from '@lvce-editor/test-with-playwright'

export const name = 'viewlet.explorer-rtl-bidi-file-names'
export const skip = 1

export const test: Test = async () => {
  // TODO arrange: create right-to-left and bidi-marker file names.
  // TODO act: select, rename, and copy path for those names in Explorer.
  // TODO assert: text rendering, selection, editing, and copied paths remain coherent.
}
