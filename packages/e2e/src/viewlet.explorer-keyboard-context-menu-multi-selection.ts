import type { Test } from '@lvce-editor/test-with-playwright'

export const name = 'viewlet.explorer-keyboard-context-menu-multi-selection'
export const skip = 1

export const test: Test = async () => {
  // TODO arrange: multi-select Explorer files with keyboard-driven focus.
  // TODO act: open the context menu from the keyboard and choose a file operation.
  // TODO assert: the operation applies to the full selection, not just the focused item.
}
