import type { Test } from '@lvce-editor/test-with-playwright'

export const name = 'viewlet.explorer-collapse-parent-clears-hidden-child-selection'
export const skip = 1

export const test: Test = async () => {
  // TODO arrange: expand a folder and select one of its child files.
  // TODO act: collapse the parent folder.
  // TODO assert: hidden child selection is cleared or ignored by later operations.
}
