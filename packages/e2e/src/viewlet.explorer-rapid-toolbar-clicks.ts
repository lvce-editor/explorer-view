import type { Test } from '@lvce-editor/test-with-playwright'

export const name = 'viewlet.explorer-rapid-toolbar-clicks'
export const skip = 1

export const test: Test = async () => {
  // TODO arrange: open a workspace with files and folders in Explorer.
  // TODO act: rapidly click toolbar actions such as refresh, collapse all, and new file.
  // TODO assert: Explorer ends with at most one edit input and no duplicated or stale rows.
}
