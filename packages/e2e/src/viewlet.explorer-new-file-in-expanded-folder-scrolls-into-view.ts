import type { Test } from '@lvce-editor/test-with-playwright'

export const name = 'viewlet.explorer-new-file-in-expanded-folder-scrolls-into-view'
export const skip = 1

export const test: Test = async () => {
  // TODO arrange: expand a folder near the edge of a scrolled Explorer viewport.
  // TODO act: create a new file inside the expanded folder.
  // TODO assert: the edit input scrolls into view and remains usable.
}
